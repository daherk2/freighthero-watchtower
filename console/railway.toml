# Build configuration for Railway Frontend
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.frontend"

# Deploy configuration
[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Environment variables
[deploy.env]
NODE_VERSION = "20"
